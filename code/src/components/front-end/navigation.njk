<nav class="max-w-[1200px] mx-auto p-4 relative" role="navigation" aria-label="Menu principal">
    <div class="flex justify-end">
        <button id="burger-btn" class="md:hidden flex flex-col gap-1.5 p-2" aria-label="Menu">
            <span class="w-7 h-0.5 bg-slate-900"></span>
            <span class="w-7 h-0.5 bg-slate-900"></span>
            <span class="w-7 h-0.5 bg-slate-900"></span>
        </button>
    </div>

    <ul id="menu-front" class="hidden md:flex flex-col md:flex-row md:justify-end gap-y-4 md:gap-x-10 mt-4 md:mt-10">
        {% for menu_item in menu %}
            <li>
                <a class="block text-lg hocus:text-pink-600 border-b md:border-none border-slate-100 py-2 md:py-0" href="{{ menu_item.link }}">
                    {{ menu_item.name }}
                </a>
            </li>
        {% endfor %}
    </ul>
</nav>